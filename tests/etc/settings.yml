
sources:
  test:
    connection:
      driver: "sqlite"
      file: "files/database.sqlite"

    entities:
      - {name: Album}
      - {name: Invoice, column: InvoiceDate}
      - {name: Playlist}
